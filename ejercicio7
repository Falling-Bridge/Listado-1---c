#include <stdio.h>

int main(){
    int n; scanf("%d", &n);
    if(n >= 0){
        printf("Su configuracion zig-zag es:\n%d\n", 2 * n);
    } else {
        printf("Su configuracion zig-zag es:\n%d\n", -(2 * n + 1));
    }
    return 0;
}
