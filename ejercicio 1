#include <stdio.h>
int main() {
    int speed, llega;
    scanf("%d", &speed);
    llega = 3700 - 9 * speed;
    if (llega <= 0) {
        printf("Llegara a tiempo a su residencia.\n");
    } else {
        printf("No llegara a tiempo a su residencia.\n");
    }
    return 0;
}

